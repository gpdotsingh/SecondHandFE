import { CarDetaiilsPipe } from './car-detaiils.pipe';

describe('CarDetaiilsPipe', () => {
  it('create an instance', () => {
    const pipe = new CarDetaiilsPipe();
    expect(pipe).toBeTruthy();
  });
});
